#include <iostream>
using namespace std;

int main()
{
    const int SIZE = 10;
    int ar[SIZE];
    
    // Input

    cout << "Enter 10 numbers: ";
    for (int i = 0; i < SIZE; i++) {
    cin >> ar[i];
    }

    cout << "Even numbers are located at indices: ";
    for (int i = 0; i < SIZE; ++i) {
        if (ar[i] % 2 == 0) {
            cout << i << " ";
        }
    } 

    return 0;
}
